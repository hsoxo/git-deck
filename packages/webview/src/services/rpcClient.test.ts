import { describe, it, expect } from 'vitest';

describe('RPCClient', () => {
    it('should be tested in integration tests', () => {
        // RPCClient requires browser environment with VS Code API
        // It's better tested in integration tests where the full environment is available
        expect(true).toBe(true);
    });
});
